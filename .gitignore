# Generated by scripts/build-pages.mjs — never committed.
/_site/

# Godot's per-project import/cache directory, regenerated from source on
# demand (the role node_modules/ plays). A project's exported web/ build
# IS committed — nothing in CI can regenerate it.
.godot/

node_modules/
